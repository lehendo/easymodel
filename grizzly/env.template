# Environment Variables Template for Vercel
# Copy these to Vercel Dashboard: Settings -> Environment Variables

# Required: Backend API URL (set after deploying backend)
NEXT_PUBLIC_EASYMODEL_API_URL=https://your-backend-url.railway.app

# Required: Database URL (use Vercel Postgres for production)
DATABASE_URL=postgresql://user:password@host:5432/database

# Optional: Direct URL (if using Prisma with connection pooling)
# DIRECT_URL=postgresql://user:password@host:5432/database

