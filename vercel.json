{
  "buildCommand": "cd grizzly && npm install && npx prisma generate && npx prisma migrate deploy && npm run build",
  "outputDirectory": "grizzly/.next",
  "installCommand": "cd grizzly && npm install"
}
